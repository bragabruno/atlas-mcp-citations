apiVersion: v1
kind: Service
metadata:
  name: {{ include "atlas-mcp-citations.fullname" . }}
  labels:
    {{- include "atlas-mcp-citations.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "atlas-mcp-citations.selectorLabels" . | nindent 4 }}
